#include "shell.h"
#define max 1024
int main ()
{
char cmd[max];
char *args[max / 2 + 1]

char tkn;
while (1)
{
printf("AH $");
fflush(stdout);
if ((fgets(cmd, max, stdin)) == NULL)
	return (-1);


}
}
